apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: travel-ai
spec:
  replicas: 2
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order
          image: ghcr.io/wovoemj/1/order-service:latest
          ports:
            - containerPort: 8083
          envFrom:
            - configMapRef:
                name: travel-ai-config
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "1Gi"
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: travel-ai
spec:
  selector:
    app: order-service
  ports:
    - port: 8083
      targetPort: 8083
